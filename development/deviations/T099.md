---
id: deviation-T099
purpose: Implementation drift log for T099.
layer: how
status: closed
ticket: T099
created: 2026-06-04
last_entry: 2026-06-04
---

# T099 — F039 Locally Made proximity substrate + public render

## 2026-06-04 — Two deviations

1. **Migration numbered `031`, not `027`.** The ticket drafted the migration as `027_place_proximity.sql`; `027` was already taken by `027_locality_feed.sql` (applied). Renumbered to `031` (next free; highest applied was 030). The ticket text was corrected to `031`. No behavioral effect.

2. **Proximity is pure Place-lineage; the metro_polygons overlay (D3) is out of scope.** Scenario beat 3 lists three OR-paths for proximity: self, hierarchy ancestor, **or** metro_polygons co-membership (D3). `place_is_proximal_to_any` implements the lineage paths (ancestor-or-self in either direction → self/ancestor/descendant proximal). Same-metro **siblings** (e.g. East Sacramento vs Oak Park) and cousins (Folsom vs Oak Park) return **false** — there is no `metro_polygons` table at b1 (only `zip_metro_crosswalk`). The metro overlay is deferred to D3 as the scenario anticipates. Narrow-by-design, not a gap. No SPEC-PATCH — the scenario already frames D3 as downstream.

**Verification:** 28/28 resolver + page unit tests GREEN; proximity function verified directly against the seeded hierarchy (self/ancestor/descendant true; sibling/cousin/empty/null false). M2 review added a depth/cycle guard to the recursive CTE (curated data is a tree, but a pathological `parent_id` loop must never hang the request).

**Disposition:** no spec divergence. M2 Approve.
