---
id: deviation-T098
purpose: Implementation drift log for T098.
layer: how
status: closed
ticket: T098
created: 2026-06-04
last_entry: 2026-06-04
---

# T098 — F037 Playwright eval + fixture

## 2026-06-04 — One deviation

1. **Fixture stamps `locations.place_id` directly.** T075 deviation #1 shipped `locations.place_id` with no population path at b1, so `zip_is_proximal_to_location` returns false for every real Location until a backfill ships. For the eval to exercise the proximal path, `evals/fixtures/F037-claim.ts` sets the anchor Location's `place_id` to the `oak-park` neighborhood Place (msa_code 40900) directly. This is a test-seed workaround, not production behavior — it tracks the already-filed T075 SPEC-PATCH (`locations.place_id` population path). No new SPEC-PATCH.

**Verification:** F037 eval 8/8 GREEN against live local Supabase (beats 1–6 + inline-validation edge); F035 badge-absent beat still GREEN (no regression). 284/284 src unit + action-layer/RLS conformance GREEN.

**Disposition:** no new spec divergence. M2 PROCEED.
