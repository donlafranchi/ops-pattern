---
id: deviation-T100
purpose: Implementation drift log for T100.
layer: how
status: closed
ticket: T100
created: 2026-06-04
last_entry: 2026-06-04
---

# T100 — F039 Locally Made claim lifecycle

## 2026-06-04 — One deviation

1. **Event kinds are `item.made_at_set` / `item.made_at_removed`, not the scenario's generic `item.updated`.** Scenario beat 5 says the edit "emits `item.updated`." T064 (migration 020) added the more-specific `item.made_at_set` / `item.made_at_removed` / `item.made_at_verified` kinds precisely for this claim. The handlers emit the specific kinds (set → `item.made_at_set`, remove → `item.made_at_removed`) — still satisfying the same-transaction row+event invariant (ADR-10), with a finer-grained audit trail than a generic update. `item.updated` remains a valid kind but is not emitted here. No SPEC-PATCH: the specific kinds are the intended encoding (T064), and the scenario's "item.updated" was generic shorthand for "an update event fires."

**Verification:** 13/13 unit tests GREEN (`MadePlacePicker`, `LocallyMadeClaim` render states, `set-made-at` input schema, `ProductComposer` made-step selection). Owner-gating, event emission, and idempotent remove verified end-to-end by the F039 eval (T101). `npm run check:action-layer` OK (288 files, 0 violations) — the two new handlers route through the action layer.

**Disposition:** no material spec divergence. M2 Approve.
