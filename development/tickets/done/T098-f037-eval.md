# T098: F037 Playwright eval + fixture (Locally Owned claim lifecycle)

**Scenario:** `planning/now/scenario-F037-maya-claims-locally-owned.md`
**Status:** Open
**Bundle:** b1 (sub-bundle b1.2 — Business Groups & makers)
**Depends on:** T096, T097

**Serves:** End-to-end verification of F037 beats 1–6 against live Supabase. Greens the jurisdiction half of `use-cases.md` P4.

## Scope

1. **Fixture — `evals/fixtures/F037-claim.ts`.** Mirrors F035-shop conventions (idempotent, race-safe, bcrypt password RPC):
   - Maya (owner) + Rosa (non-owner) identities via `eval_seed_auth_user_with_password`.
   - One **active** kind='business' Group ("Oak Park Sourdough — F037") with Maya as owner-role member.
   - Anchor Location with `place_id` set to the `oak-park` neighborhood Place (which carries `msa_code='40900'` per migration 025), so `zip_is_proximal_to_location` can resolve. **This is the key delta from F035** — F035's location left `place_id` null; F037 needs it populated for proximity to pass.
   - `resetJurisdiction(mayaMemberId, groupId)` helper — hard-deletes Maya's jurisdiction rows for the Group, restoring empty state. Called in `beforeEach`.

2. **Spec — `evals/features/F037-maya-claims-locally-owned.spec.ts`.** `test.describe.configure({ mode: 'serial' })` + `beforeEach` reset (mutating surface; same pattern as F036). Beats:
   - **Beat 1.** Owner (signed-in Maya) sees `data-testid="claim-add"` widget on her Shop; anonymous viewer of the same URL does NOT (`toHaveCount(0)`).
   - **Beat 2.** Maya adds `95817` → claimed+proximal state renders + `data-testid="local-owner-badge"` appears on reload.
   - **Beat 3.** Maya edits to `95816` → badge persists.
   - **Beat 4.** Maya removes → empty state returns + badge gone (`toHaveCount(0)`).
   - **Beat 5.** Maya submits `90210` → `data-testid="claim-not-proximal"` shows + badge absent.
   - **Beat 6.** Rosa (signed-in non-owner) at the Shop URL: no `claim-add` / `claim-edit` widget.
   - **Edge.** Non-5-digit ZIP → `data-testid="claim-zip-error"`, no row written.

## Acceptance Criteria

- [ ] Fixture seeds an active Shop with a `place_id`-anchored Location (MSA 40900) + Maya owner + Rosa non-owner; idempotent across reruns.
- [ ] All six beats + the inline-validation edge pass against live Supabase (`npm run eval -- --grep "F037"`).
- [ ] No regression in F035 (badge-absent beat still green — F035's Shop has no jurisdiction).
- [ ] **DEVIATIONS** entry at close. **STAGE-LEDGER** F037 row stamped to `test-green`.

## Notes

- ZIPs 95816/95817/95818 are seeded in `zip_metro_crosswalk` (MSA 40900); 90210 is NOT seeded → null-safe-false → non-proximal. Verified in migration 025.
- Serial mode + per-test reset is required: all beats mutate Maya's single Group jurisdiction (unique-active-row constraint), so parallel workers would race.
