---
purpose: Marker file. Approved scenarios live here.
layer: how
status: active
---

# planning/scenarios/ — approved scenarios

> Scenarios ratified by `pipeline-plan` and moved here by the PM. `pipeline-eval` (write mode) and `pipeline-ticket` read **only** from this directory.
>
> Empty is a valid state — but if any open ticket carries a `Scenario: planning/scenarios/F###`, the F### file **must** exist here. `pipeline-router`'s drift check flags violations.
>
> Drafts live in `../scenarios-backlog/` until approved.
