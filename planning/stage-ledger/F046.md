---
id: stage-F046
purpose: Pipeline stage for F046 — scroll-to-hide bottom navigation (global).
layer: how
status: active
concept_kind: feature
stage_current: built
last_activity: 2026-09-02
---

# F046 — Member scrolls and nav hides

**Spec contract:** design-language.md #9, community-platform.md (global nav)
**Anchor:** use-cases.md C1

## Stage history (append-only)

- **2026-09-02** · `plan-backlog` — drafted by `scope`. Global interaction change — bottom tab bar hides on scroll down, reappears on scroll up. No schema change.
- **2026-09-02** · `plan-backlog` — revised by `scope`. Grounded in thesis §2 nav spec: 52px height, charcoal-700 active state (#3C3C3C, not pistachio), #717171 inactive, white bg, no blur. Added open question: do kind-filter pills (F045) stay fixed when nav hides? Recommendation: pills stay (frequent-tap vs infrequent tab-switch). PM to ratify.
- **2026-09-02** · `plan-approved` — PM ratified; moved `backlog/` → `next/`. Gate A clear (no schema; nav height + pill-stay decisions ratified same day). Open question on pill behavior resolved (pills stay).
- **2026-09-02** · `ticketed` — T112–T113 written by `ticket`. Gate B clear (no unratified absolutes; 44px nav height ratified same day). T112 is the dependency root for the F044/F045/F046 ticket chain.
- **2026-09-02** · `built` — T112 (nav visual refresh) implemented by `build` on branch `t112`. 44px bar, charcoal #3C3C3C active, 20px/1.5-stroke icons with filled active glyph, 9px/500 labels, #EBEBEB hairline, opaque white. M2 code-review caught two defects pre-commit (stale `64px` sticky-CTA offsets on the vendor + business pages; `--nav-height` silently dropped by Tailwind v4's `@theme inline`). M3 accessibility PASS with two fixes (full-height touch targets, named nav landmarks). **T113 (scroll-to-hide behavior) still open** — F046's core interaction is not yet built; T112 covered only the visual-treatment criterion.

- **2026-09-02** · `built` — T113 (scroll-to-hide behavior) implemented by `build` on branch `t113`. F046's core interaction now ships: `useScrollDirection` (passive listener, rAF-throttled, 20px sustained-travel threshold, direction-flip debounce), `NavVisibilityProvider` publishing `navVisible` through context for T114's pill row, `translate-y-full` slide with 200ms ease-out and `motion-reduce` opt-out. Auto-hide pauses on any `[aria-modal="true"]`; the bar hides while the virtual keyboard is up; a route change restores it. Kind-filter pills stay fixed per the 2026-09-02 ratification — verified live on `/explore`. M2 code-review clean (two clarifications applied pre-commit). M3 accessibility PASS, no new findings — the bar keeps its landmark in the a11y tree while off-screen and `focus-within` returns it the moment a tab link takes focus. **F046 fully built; awaiting `test` (eval run).**
