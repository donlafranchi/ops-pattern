---
id: stage-F033
purpose: Pipeline stage for F033 — Viewer finds venue page.
layer: how
status: active
concept_kind: feature
stage_current: ticketed
last_activity: 2026-06-16
---

# F033 — Viewer finds venue page

**Spec contract:** location.md, member.md saved-searches; ADR-21
**Anchor:** use-cases.md O1 + O2 (viewer side)

## Stage history (append-only)

- **2026-05-28** · `plan-backlog` — drafted by `scope`
- **2026-06-15** · `plan-backlog` — rewritten by `scope`: Host/Venue distinction, venue-scoped "What's happening here" filter, "What's happening nearby" secondary affordance, private-event exclusion, distance from `member_place_interests` primary-home centroid (not vestigial `home_location_id`), "Follow this venue" promoted to primary CTA. Gate A PASS. Added Loops 3 + 8, sub-bundle b1.3, work-map item, `_Why:` annotations on all non-obvious clauses.
- **2026-06-15** · `plan-approved` — PM moved scenario to `planning/next/`.
- **2026-06-15** · `reviewed` — review-F033 verdict PROCEED. All schema exists, Host/Venue filtering uses existing FKs, DLS § Venue page needs CTA hierarchy update (mandatory pre-condition for first ticket).
- **2026-06-16** · `ticketed` — T104 (venue page shell: route, header, CTAs, About, auth gating) + T105 (content sections: "What's happening here" + "What's happening nearby"). 2 tickets, sequenced T104 → T105. Gate B PASS — no new absolutes encoded.

## Notes

New scenario — venue page was assumed-to-exist in F018. **S-saved-search substrate gate** (for "Follow this venue" CTA).

- **2026-06-11** — S-saved-search substrate gate **CLOSED** by T102: `<FollowVenueButton>` (`web/src/components/venue/`) + `followVenueAction`/`unfollowVenueAction` (`web/src/app/_actions/saved-search-actions.ts`) are built and ready to drop into the venue page. F033 owns the route, data fetch, layout, and the `existingSavedSearchId` server-render. Stage unchanged (`plan-backlog`) — PM promotes the scenario when ready.
