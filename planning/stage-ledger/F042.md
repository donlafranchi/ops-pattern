---
id: stage-F042
purpose: Pipeline stage for F042 — Member follows producer, Group, venue.
layer: how
status: active
concept_kind: feature
stage_current: ticketed
last_activity: 2026-06-18
---

# F042 — Member follows producer, Group, venue

**Spec contract:** member.md follows, groups.md, member.md saved-searches
**Anchor:** use-cases.md C1

## Stage history (append-only)

- **2026-05-28** · `plan-backlog` — drafted by `scope`
- **2026-06-18** · `plan-approved` — scenario revised during review (`unfollowed_at` column-name fix, Leave vs Unfollow for Groups, two-level surface), moved → `planning/next/` (PM-directed)
- **2026-06-18** · `reviewed` — review-F042 verdict PROCEED (all substrate shipped; two-level surface pattern: card scroll on `/you` + full page at `/you/following`)
- **2026-06-18** · `ticketed` — T108 (unified follows reader + `/you` Following card-scroll summary + DLS recipe) · T109 (`/you/following` management page — People/Groups/Venues sections, Unfollow/Leave, undo/re-activate, count-privacy). Gate B PASS — no new absolutes (pure reads over shipped substrate + reuse of existing `member.unfollow` / `group.member_leave` / `member.saved_search.remove` handlers). No migration.

## Notes

New scenario — cross-cutting follow CTA tests (Member follow tested in F032; this covers Group + Venue + the unified `/you/following` surface). Two tickets per review's "combine where data queries are shared": T108 owns the unified reader + `/you` summary; T109 reuses it for the management page.

- **2026-06-11** — Venue-follow leg of the substrate gate **CLOSED** by T102 (`followVenueAction`/`unfollowVenueAction` + `<FollowVenueButton>`).
