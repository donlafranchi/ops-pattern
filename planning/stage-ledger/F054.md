---
id: stage-F054
purpose: Pipeline stage for F054 — canonical Item URLs and browsable-kind withholding.
layer: how
status: active
concept_kind: feature
stage_current: built
last_activity: 2026-09-04
---

# F054 — A member taps something in the feed and lands on it

**Spec contract:** PLATFORM-PATTERNS.md § *An Item's canonical URL is its Group place-path when filed, its Member path when not* · § *Browse surfaces link only Item kinds that have a detail page* · CLAUDE.md § Naming conventions (URL column)
**Anchor:** use-cases.md C1

## Stage history (append-only)

- **2026-09-04** · `plan-approved` — written by `scope` and approved into `next/` in one pass. Upstream was a ratified decision (`decision-item-canonical-urls.md`, resolved same day) rather than a backlog draft, so Gate A was satisfied at ratification: both absolutes carry State tags, and the deferred half (Group-segment `id8`) carries a trigger and a horizon.
- **2026-09-04** · `reviewed` — **PROCEED** with three binding notes: reuse the county-skipping convention rather than restate it; make the attribution fallback uniform across all three resolvers, not gathering-only; keep one list of browsable kinds consumed by every read path. M3 ran — no new interactive element, no contrast surface; two checks deferred to build (accessible names unaffected by the href change; an emptied list must announce as empty rather than render silently). Sibling check flagged that F045's filter facets must read the browsable list rather than hardcode kinds — which turned out to apply to T114's *shipped* pill row too.
- **2026-09-04** · `ticketed` — T119 written by `ticket`. Gate C clear (`review-F054.md` present — the gate F044 and F045 both skipped). Gate B clear.
- **2026-09-04** · `built` — T119 implemented by `build` on branch `t119`, merged and deployed. Found a **third** 404 cause the decision stub had not identified: Group attribution read `items.brand_label`, populated only for business Groups, so a Group event filed under a non-business Group 404'd at the resolver *even with a correct URL* — which would have taken the whole v1 Group-events workstream down. Fixed uniformly (`brand_label ?? groups.name`). All three binding notes honoured. Three shipped Explore kind pills were withdrawn with their kinds (the review's sibling flag, applied to T114's live row) — `KIND_FILTERS` is now derived so the two cannot drift apart again. M2 code-review caught two defects pre-commit: a browsable constraint applied only on the All path (relying on an upstream parse guard), and an unbounded ancestor walk in `place_url_path` that a `places` cycle could have hung the feed on. 677 unit tests green (one pre-existing red unchanged); build clean; lint at baseline.
- **2026-09-04** · `built` — verified on production, not just locally. Every Item linked from Home and Explore returns 200 (11 of 11); the four Group-filed rows resolve at their real place-paths; the Repair Cafe page renders "Hosted by Repair Cafe Regulars"; a Member whose only Items are withheld kinds shows the honest empty state. **Awaiting `test` (eval run) — the F038/F034 eval extension in T119's test plan was not written.**
