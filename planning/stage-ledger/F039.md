---
id: stage-F039
purpose: Pipeline stage for F039 — Producer claims Locally Made (Tier 0).
layer: how
status: active
concept_kind: feature
stage_current: deferred
last_activity: 2026-06-18
---

# F039 — Producer claims Locally Made (Tier 0)

**Spec contract:** item.md Provenance, places.md
**Anchor:** use-cases.md P4 (provenance half)

## Stage history (append-only)

- **2026-05-28** · `plan-backlog` — drafted by `scope`
- **2026-06-01** · `plan-backlog` (cont.) — scenario drafted (`scenario-F039-maya-claims-locally-made.md`; Gate A PASS pending `item.md` § Provenance re-check at substrate ship)
- **2026-06-02** · S-jurisdictions substrate gate CLOSED (T075 built; `made_at_place_id` already in 020, jurisdiction substrate + crosswalk shipped)
- **2026-06-04** · F038 substrate gate CLOSED (composer + page shipped); promoted `backlog/` → `now/`; built T099 (proximity substrate `place_is_proximal_to_any` migration 031 + `madeAtPlaceName` read + `resolveMadeLocallyBadge` + "Made in {place}" line + proximity-gated badge) + T100 (`item.made_at.set`/`.remove` handlers + `<MadePlacePicker>` + `<LocallyMadeClaim>` widget + composer step-3 picker) + T101 (eval + fixture). **F039 eval 6/6 GREEN** on branch `t-f039` (**not merged**). F038 beat 4 forward-fixed (badge → proximity-gated). M2 Approve (cycle-guard fix). Greens the provenance half of use-cases P4 once merged.
- **2026-06-18** · `deferred` (PM) — Deferred: Locally Made claim needs redesign to work across product categories, not just kind='product'. Branch `t-f039` deleted (was `f8ad8dc`, local-only — never pushed). Work to be re-approached.

## Notes

**Two substrate gates**: S-jurisdictions (`made_at_place_id` column + `places` seed) — **CLOSED 2026-06-02 (T075 built; `made_at_place_id` already in 020, jurisdiction substrate + crosswalk shipped)** — AND F038 (product composer — the surface F039's field lives on). Cannot promote until F038 also ships. Greens the provenance half of `:266` once all three land.
