---
id: stage-F059
purpose: Pipeline stage for F059 — Explore absorbed into Home; one browse surface.
layer: how
status: active
concept_kind: feature
stage_current: ticketed
last_activity: 2026-09-04
---

# F059 — A newcomer browses one surface instead of two

**Spec contract:** decision-surfaces.md § The two-tab model / § Feed ranking / § Distance is out / § Metro is the vantage point; community-platform.md § T1 Home + § T1 Explore
**Anchor:** use-cases.md C1

## Stage history (append-only)

- **2026-09-04** · `plan-backlog` — drafted by `scope` against bundle-1 workstream 4. Direction was already ratified (Explore absorbs Home); this scenario scopes it against the v1 constraints — metro-only, distance removed, server-side ranking, two-tab nav. **Drafted first as F054 and renumbered same session** — a parallel session had already taken F054 for the canonical-Item-URL work (T119, shipped). No content was lost; see Notes.
- **2026-09-04** · Gate A **FAILED** — two absolutes the scenario would encode carried no State tag: `community-platform.md:70` (anonymous browse, no signup wall) and `community-platform.md:64` ("No personalization algorithm at T1"). Routed to `weigh`; scenario held in `backlog/`.
- **2026-09-04** · `weigh` — both ratified with the PM. Anonymous browse ratified as written and widened to include "no gated or truncated result set." The personalization absolute ratified in **corrected** form — it had already drifted, since `locality_feed_items` has boosted on `member_interests` since T087; the surviving refusal is behavioural/engagement-derived ranking, not a Member's own declared tags. Both `Intent` lines landed in `community-platform.md`.
- **2026-09-04** · PM directive at merge scope — **v1 filters by metro.** The "ranking rule, not a filter rule / nothing excluded" entry in `decision-surfaces.md` § Feed ranking is **demoted from a standing commitment to a versioned bet** with a revisit trigger, per the PM's instruction that "these are the kind of decisions that need to be removed from global decisions and entered into this version's decision matrix." Amendment landed in `decision-surfaces.md`; the worked reasoning landed as `decision-durability-register.md` § 9b. **Consequence:** the metro vantage point becomes a hard dependency of the merge rather than a neighbouring workstream, because `places.kind` has no metro value and metro is a separate overlay table.
- **2026-09-04** · Gate A **PASSED** on re-run; `scope` advanced `backlog/` → `next/`.
- **2026-09-04** · `reviewed` — **REVISE** (`planning/next/review-F059.md`). Architecture PROCEEDs with two named substrate conditions: the feed RPC projects three columns short of what the browse controls need (search, schedule filter and map are dead until it widens), and the metro vantage point is a second function against a different table rather than a parameter change. Design REVISEs on one clause: the scenario claimed the chrome ports "unchanged in appearance," which relocates a live `design-language.md` principle 9 violation (`ExploreSearchBar` is `sticky top-0`; principle 9 says "No top-anchored toolbars or search fields") onto the platform's landing surface. **Revision applied same day** — the violation is now recorded as knowingly accepted for one release with the follow-on chrome scenario named as the remedy. M3 raised four accessibility findings on the composed surface; all four carried into tickets.
- **2026-09-04** · `ticketed` — T127–T131 by `ticket`. **Gate C passed** — `review-F059.md` present, which is the check F044 and F045 both failed. Gate B clear: every absolute the tickets encode carries a Ratified tag, and `design-language.md` principle 9 is owed no tag (the durability register already classifies all nine DLS principles as bets). Sequence: RPC projection → metro vantage point → the merged surface → URL/scroll/redirect → two-tab nav.

## Notes

**Finding this review produced that the prior analysis did not have.** `resolveFeedPlace` returns on `memberPlaceId` before it ever reads `requestedSlug`, so `?place=` is inert for exactly the Members who have a home set — the shipped scope picker works only for signed-out visitors. The merged surface makes the switcher load-bearing, so the precedence inverts in T128.

**Standing consequence for the other three.** F059 is the first of F044/F045/F046 to be reviewed, and the principle 9 finding is the direct cost of the other two not being: T115 shipped a top-anchored search row citing `design-research-thesis.md` §5, and no gate ever put the thesis and the design language in the same room. Recommended remedy is a precedence banner on the thesis, not two retroactive reviews of shipped work.

**F-number collision, and the gap it points at.** This scenario was drafted as F054 while a parallel session was concurrently taking F054 for the canonical-URL work. `scope`'s numbering rule ("check `backlog/`, `next/`, `now/` for the highest number") missed it because that session had already archived its scenario to `planning/done/`, which the rule does not name. **Two sessions following the rule correctly can collide.** Cheapest fix is to add `planning/done/` and `planning/stage-ledger/` to the scan — the ledger is the one place every F-number persists regardless of lane.

**The collision was already in HEAD, not just in flight.** `planning/backlog/scenario-F054-newcomer-browses-one-surface.md` — this scenario's first draft, under the wrong number — was committed in `62a461e`, the same commit that added F054's *actual* scenario (canonical Item URLs). So F054 was double-booked in the repository itself, and the session-start working tree showed a half-finished cleanup of it (a staged `backlog/` → `next/` rename with the target deleted). The stale path is removed in the commit that lands F059. **This raises the numbering fix from a nice-to-have to a correction of live repo state:** the ledger scan would have caught it, the lane scan did not, and the wrong-numbered copy survived a commit and a close-out.

**Cut-friendliness, recorded deliberately.** T127 and T128 are v1 workstream 8 (metro-only location), which the bundle protects. T129–T131 are the merge itself, which the bundle lists as its third cut. The split is along that line on purpose: cutting the merge drops three tickets and leaves the metro work standing.
