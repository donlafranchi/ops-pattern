> **Moved.** The ticket template now lives at `skills/ticket/templates/ticket.md`.
>
> The build agent does not write tickets — `ticket` does. This stub remains only because the build session running this codebase cannot delete files. Safe to remove manually.
